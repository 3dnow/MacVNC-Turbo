# MacVNC-Turbo
MacVNC-Turbo is a zero-dependency, ultra-low latency VNC solution designed specifically for controlling macOS from a Windows machine. It bridges the native WebSockets with TCP and eliminates the notorious Apple Auth protocol limitations and modifier key conflicts (Ctrl vs. Cmd).
